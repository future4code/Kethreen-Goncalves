function checkAge ( data ) {
    if ( data === { idade : 18 } ) {
    console . log ( 'Você é um adulto!' ) ;
    } else if ( idade == { age : 18 } ) {
    console . log ( 'Você ainda é um adulto.' ) ;
    } else {
    console . log (`Hmm .. Você não tem idade, eu acho` ) ;
    }
    }
    
    checkAge ( { idade : 18 } ) ;

    
    